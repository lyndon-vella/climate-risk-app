# Backend Configuration
# Currently uses free APIs that don't require keys

# Optional: Add if you want to use Google Elevation API (more reliable)
# GOOGLE_MAPS_API_KEY=your_google_api_key

# Server settings
HOST=0.0.0.0
PORT=8000
